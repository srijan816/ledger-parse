/**
 * This file exists for client-side PDF viewing only.
 * Server-side PDF processing now uses pdf-parse directly.
 * 
 * Note: Do NOT import this file in server-side code (API routes, etc.)
 * as it will cause worker initialization errors with Next.js Turbopack.
 */

// This is only used for client-side PDF viewer component
// The file is intentionally left minimal to avoid import issues
export { };
